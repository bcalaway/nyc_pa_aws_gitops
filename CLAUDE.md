# Claude Code Instructions

## gh CLI setup (new machine)

The GitHub API token is stored in SSM at `/home-platform/github/api-token`. Full step-by-step for both Windows and Linux (Rocky NUCs) machines is in `docs/new-machine-setup.md` — this is just the quick version.

**Windows:**
```powershell
# Install gh CLI
winget install --id GitHub.cli --accept-package-agreements --accept-source-agreements

# Auth from SSM (open a new terminal after install so PATH is updated)
$token = (aws ssm get-parameter --name "/home-platform/github/api-token" --with-decryption --region us-east-1 --output json | ConvertFrom-Json).Parameter.Value
$token | & "C:\Program Files\GitHub CLI\gh.exe" auth login --with-token
```

**Linux (Rocky/RHEL, dnf-based — the NUC pattern):**
```bash
# Install gh CLI
sudo dnf config-manager --add-repo https://cli.github.com/packages/rpm/gh-cli.repo
sudo dnf install -y gh

# Auth from SSM
token=$(aws ssm get-parameter --name "/home-platform/github/api-token" --with-decryption --region us-east-1 --output json | jq -r '.Parameter.Value')
echo "$token" | gh auth login --with-token
gh auth setup-git   # required if the repo was cloned over HTTPS (the norm on Linux) so `git push` has credentials
```

After setup, use `gh run list --repo bcalaway/nyc_pa_aws_gitops` to check Actions runs.

## Git

Always `git push` immediately after every `git commit` without asking.

## AWS

- Account: 147856894209
- Region: us-east-1
- Terraform state bucket: `home-platform-terraform-state-147856894209`
- GitHub Actions IAM role: `home-platform-github-actions`

## SSM parameters

| Path | What it is |
|------|-----------|
| `/home-platform/github/api-token` | GitHub PAT for gh CLI |
| `/home-platform/router/nyc-admin-password` | NYC RB5009 admin password |
| `/home-platform/router/rambles-admin-password` | Rambles RB5009 admin password (set when hardware arrives) |
| `/home-platform/wireguard/server-private-key` | EC2 WireGuard hub private key |
| `/home-platform/wireguard/laptop-private-key` | Laptop WireGuard client private key |
| `/home-platform/wireguard/laptop-public-key` | Laptop WireGuard client public key |
| `/home-platform/grafana/admin-password` | Grafana admin login |
| `/home-platform/grafana/smtp-password` | Grafana Gmail SMTP App Password (real value set, verified working 2026-07-18) |
| `/home-platform/uptime-kuma/admin-password` | Uptime Kuma admin login |
| `/home-platform/postgres/admin-password` | Shared Postgres instance (hub) superuser password — apps get their own per-database least-privilege credentials under this same `/home-platform/postgres/*` namespace as they're onboarded |
| `/home-platform/authentik/redis-password` | Redis (hub) password — Authentik's dependency (ADR-0017), namespaced under Authentik's own SSM path since Redis has no other consumer |
| `/home-platform/switch/nyc-sw-desk-username` | sw-desk (Cisco SG300-10, 10.0.1.11) admin username — not "admin", a personal account |
| `/home-platform/switch/nyc-sw-desk-password` | sw-desk admin password |
| `/home-platform/switch/nyc-sw-main-username` | sw-main (Cisco SG300-10, 10.0.1.10) admin username |
| `/home-platform/switch/nyc-sw-main-password` | sw-main admin password |
| `/home-platform/switch/nyc-sw10g-username` | sw-10g (MikroTik, 10.0.1.12) admin username |
| `/home-platform/switch/nyc-sw10g-password` | sw-10g admin password |
| `/home-platform/nas/nyc-nas2-username` | nas2 (Synology, 10.0.1.7) admin username |
| `/home-platform/nas/nyc-nas2-password` | nas2 admin password |
| `/home-platform/kvm/admin-password` | JetKVM web UI password — shared by both kvm-nuc4 (10.0.1.66, NYC) and kvm-nuc5 (10.0.2.226, Rambles); no separate username field, JetKVM auth is password-only |
| `/home-platform/hue/nyc-api-key` | Local CLIP API key ("username") for the NYC Hue bridge (`hue-nyc`, 10.0.1.71) — minted 2026-08-20 via `POST /api` with `devicetype=hue-controller#nyc-agent` within the ~30s window after the bridge's physical link button was pressed. Verified live against `GET /api/<key>/lights`. Same key works for both the legacy CLIP v1 endpoints (plain HTTP) and CLIP v2 (`https://<bridge-ip>/clip/v2/...`, self-signed cert, key passed as the `hue-application-key` header) — see the Hue lighting controller plan (Milestone 12, not yet written up as of this entry) |
| `/home-platform/hue/rambles-api-key` | Local CLIP API key ("username") for the Rambles Hue bridge (`hue-rambles`, 10.0.2.244) — minted 2026-08-21 via `POST /api` with `devicetype=hue-controller#rambles-agent` within the ~30s window after the bridge's physical link button was pressed. Verified live against `GET /api/<key>/lights` |
| `/home-platform/nuc/rambles-nuc5-username` | nuc5 (Rambles NUC, 10.0.2.10) SSH username (`bcalaway`) — password auth, still valid as a fallback |
| `/home-platform/nuc/rambles-nuc5-password` | nuc5 SSH/sudo password |
| `/home-platform/ansible/nuc-private-key` | SSH private key Ansible uses to manage NUCs (key-based, passwordless sudo configured for `bcalaway`) |
| `/home-platform/ansible/nuc-public-key` | Matching public key — already installed in nuc5's `authorized_keys`; add to any new NUC the same way |
| `/home-platform/github/nuc-ssh-private-key` | Shared ed25519 key for `bcalaway`'s own `git@github.com:` SSH access from the NUCs (as `~/.ssh/id_ed25519`) — registered on GitHub as "nuc key". One keypair shared across nuc4/nuc5, same pattern as the Ansible key above. Not needed on nuc4 unless its clone is switched from HTTPS to SSH — see the "New machine checklist" note |
| `/home-platform/github/nuc-ssh-public-key` | Matching public key |
| `/home-platform/rachio/api-key` | Personal API key for Bill's Rachio account (get from the Rachio app/web account settings page) — used by `rachio-exporter` (`compose/aws/rachio-exporter/`) to poll per-valve watering history for the weather dashboard |

## Ansible NUC provisioning

`ansible/` provisions the NUCs (base system, Docker, exporter stack from `compose/nuc/`). Ansible's control node doesn't support Windows, and neither a Windows workstation nor a bare Linux workstation without Ansible installed can run playbooks locally, so they run **from the EC2 hub**, which already has WireGuard routes to both site LANs and has `ansible-core` installed on demand.

```powershell
scripts/deploy-nucs.ps1    # Windows
```
```bash
scripts/deploy-nucs.sh     # Linux — same logic, kept in sync with the .ps1 version
```

This fetches the Ansible NUC private key from SSM, installs `ansible-core` on EC2 if missing, copies `ansible/` and `compose/nuc/` there, and runs `ansible-playbook site.yml` over SSH. Both NUCs are in `ansible/inventory/hosts.yml` (`nuc4`/NYC at 10.0.1.34, `nuc5`/Rambles at 10.0.2.10).

To add a new NUC to Ansible management: install the public key from SSM (`/home-platform/ansible/nuc-public-key`) into its `authorized_keys`, and configure passwordless sudo for its admin user (see the `bcalaway-ansible` sudoers drop-in on nuc5 for the pattern).

## New machine checklist

On a fresh machine (Windows or Linux — `docs/new-machine-setup.md` covers both), read that doc first for the full step-by-step. Key things to verify before starting work (same commands on both platforms):

```
aws sts get-caller-identity                          # creds valid?
gh run list --repo bcalaway/nyc_pa_aws_gitops        # gh authed?
```

If gh isn't authed yet, see the "gh CLI setup" section above.

## EC2 access

- IP: `3.82.89.106`, user: `ec2-user`
- SSH key in SSM at `/home-platform/ec2/ssh-private-key` → save to `~/.ssh/home-platform.pem`
- SSH is only open from WireGuard subnets. The laptop WireGuard peer was re-provisioned 2026-07-07 with a fresh keypair (private key in SSM at `/home-platform/wireguard/laptop-private-key` and imported into the local WireGuard app as tunnel `laptop-wireguard`; never committed to Git). If working from a device already on the NYC or Rambles LAN, that site's RB5009 also routes to the hub automatically, no client needed — but make sure the local `laptop-wireguard` tunnel is deactivated first if so, since an active tunnel takes priority for the `10.0.3.0/24` route and will break connectivity if its key is ever revoked again.
- **When connecting over the WireGuard tunnel, SSH to `10.0.3.1`, not the public IP `3.82.89.106`.** The laptop tunnel's `AllowedIPs` only covers `10.0.1.0/24, 10.0.2.0/24, 10.0.3.0/24` — traffic to the public IP goes out the normal internet path instead of the tunnel and gets blocked by the security group.

```powershell
ssh -i "$HOME\.ssh\home-platform.pem" ec2-user@10.0.3.1
```

## Deploying the AWS stack

`scripts/deploy-aws-stack.ps1` (Windows) / `scripts/deploy-aws-stack.sh` (Linux) push `compose/aws/` to the EC2 hub and bring the stack up — Prometheus, Grafana, Loki, Uptime Kuma, Authentik, Traefik, Postgres, Redis. Both fetch the same secrets from SSM into a generated `.env`, delete remote files that no longer exist locally (see the Gotchas entry on why this matters), `scp` the compose dir over, and run `docker compose pull && docker compose build && docker compose up -d`. Keep the two scripts' deploy logic in sync when changing one.

```powershell
scripts/deploy-aws-stack.ps1    # Windows
```
```bash
scripts/deploy-aws-stack.sh     # Linux
```

## Ingress / TLS on EC2 hub (Traefik, since 2026-07-18)

Traefik replaced hand-edited nginx as ingress (Milestone 11, ADR-0018) — this is now tracked in Git (`compose/aws/docker-compose.yml`'s `traefik` service) instead of host-level config, closing the "redo manually if the instance is ever rebuilt" gap the old nginx setup had.

- Routes are declared as Docker labels on each service (`grafana`, `uptime-kuma`, `authentik-server` in `compose/aws/docker-compose.yml`) — a new app gets routing for free by adding its own `traefik.*` labels, no manual hub-side edit or PR against this repo needed per app (the actual point of ADR-0018)
- `providers.docker.exposedByDefault=false` — a service is only routed if it explicitly opts in with `traefik.enable=true`; being on the same compose network isn't enough
- **Gotcha, confirmed live 2026-07-18**: `exposedByDefault=false` means Traefik ignores *all* labels on a non-enabled container, not just router creation — a shared middleware defined only on the `traefik` container's own labels (`hsts`) silently failed to register (`middleware "hsts@docker" does not exist"`) until `traefik.enable=true` was added to that container too, even though it has no router of its own
- TLS via Let's Encrypt DNS-01 through the `route53` certificate resolver — no static AWS keys, falls back to the hub's own IMDS instance-role credentials (same `home-platform-hub` role, same Route53-write-on-our-zone scope, that certbot used to use)
- Cutover sequence used (and the template for any future ingress change): stood Traefik up on alternate ports (8080/8443) first, confirmed real Let's Encrypt certs issued and routing correct via `curl --resolve` against those alternate ports with nginx still holding 80/443, *then* stopped nginx and switched Traefik to 80/443 — never ran both bound to the same ports
- nginx and `certbot-renew.timer` are now `disabled` (not removed — kept as a rollback reference for a bit). If reviving nginx is ever needed, its old config is still findable in this file's git history (see the pre-2026-07-18 version) and at `/etc/nginx/conf.d/home-platform.conf` on the hub itself
- HSTS (`max-age=31536000; includeSubDomains`) applied via a shared `hsts` middleware, referenced by each router as `hsts@docker`

## Syslog receiver on EC2 hub

Also host-level, not tracked in Git (same caveat as nginx/certbot above).

- `rsyslog` (native package, not containerized) listens on UDP 514, config at `/etc/rsyslog.d/network-devices.conf`
- Writes one file per source IP to `/var/log/network-devices/<ip>.log` — Promtail (in the compose stack) bind-mounts that directory read-only and tails it, with one explicit `static_configs` entry per known device mapping IP → friendly device name (see `compose/aws/promtail/promtail-config.yaml`)
- rsyslog owns port 514 natively; Promtail's container does **not** publish that port (it did originally, using Promtail's own built-in syslog receiver, but that only supports RFC5424 and choked on RouterOS/Cisco's legacy BSD syslog — see Gotchas below)
- Devices are pointed at `10.0.3.1:514` (the hub's WireGuard IP) via each device's own remote-syslog config — RouterOS `/system logging`, Cisco `logging host`, Synology DSM's Log Center

## Host-level security on EC2 hub

Also host-level, not tracked in Git (same caveat as nginx/certbot and rsyslog above). Reviewed and hardened 2026-07-11 as a follow-up to the AWS-level security group/IAM review (see recent git log) — that review covered the network perimeter only, not the instance itself.

- **SSH (`/etc/ssh/sshd_config`)**: `PasswordAuthentication no`, `PermitRootLogin without-password` (key-only, no root password path), `PubkeyAuthentication yes`, `KbdInteractiveAuthentication no` — this is AL2023's out-of-the-box cloud-init default from the AMI, not something added manually. Confirmed via `sshd -T`, not just by reading the file.
- **fail2ban**: installed 2026-07-11 (`dnf install fail2ban`, package is in the base `amazonlinux` repo, no EPEL needed). Config at `/etc/fail2ban/jail.local` (local override, `jail.conf` untouched) — `sshd` jail only, `maxretry=5`, `findtime=10m`, `bantime=1h`, `backend=systemd`. Deliberately does **not** whitelist the site LAN/WireGuard subnets in `ignoreip` (only `127.0.0.1`/`::1`) — SSH is already restricted to those subnets at the security-group level, so whitelisting them there too would make the jail a no-op; the point is defense-in-depth against a compromised/misbehaving internal host or leaked key. Ban action is the default `iptables-multiport`, which inserts its own dedicated chain and does not conflict with Docker's iptables-managed chains (`DOCKER`, `DOCKER-USER`, `DOCKER-ISOLATION-STAGE-*`) — verified both chain sets coexist after enabling. `systemctl enable --now fail2ban`.
- **firewalld**: pulled in as a dependency of the fail2ban package (AL2023's fail2ban RPM depends on `firewalld`/`nftables`, even though the jail itself is configured to use `iptables-multiport`, not firewalld, as the ban action). It was NOT started, and was explicitly `systemctl disable`d so it can't accidentally activate on a future reboot and reconflict with Docker's iptables rules. There is otherwise **no host-level firewall** — the AWS security group (`terraform/aws/security_groups.tf`) is the sole perimeter control, which is an intentional, acceptable pattern for this box and was left as-is (adding firewalld/nftables rules on top risks breaking Docker's own iptables-based container networking and was out of scope for this pass).
- **Docker port exposure**: `docker ps` / `ss -tlnp` confirmed every container publishes its port on `0.0.0.0` (Docker's default `docker-proxy` behavior) — node-exporter 9100, snmp-exporter 9116, prometheus 9090, cost-exporter 9199, loki 3100, grafana 3000, uptime-kuma 3001, matching `compose/aws/docker-compose.yml` exactly, nothing extra. Actual internet reachability is gated entirely by the security group: only 80, 443, 51820 (WireGuard) are open to `0.0.0.0/0`; 3001 (Uptime Kuma), 9090 (Prometheus), 3100 (Loki) are scoped to `10.0.3.0/24`; and 3000/9100/9116/9199 aren't opened in the security group at all, so they're unreachable from outside despite the container binding to `0.0.0.0`. No mismatch found.
- **nginx (`/etc/nginx/conf.d/home-platform.conf`)**: confirmed it only proxies `grafana.billandjessie.com` → `127.0.0.1:3000` and `status.billandjessie.com` → `127.0.0.1:3001`, nothing else. Both HTTPS server blocks send HSTS. No explicit `ssl_protocols`/`ssl_ciphers` directive, so it runs on nginx's compiled default; confirmed nginx version is 1.30.2 (modern default is TLSv1.2+TLSv1.3 only), so this is fine as-is.
- **Unattended security updates**: `dnf-automatic` was not installed; installed 2026-07-11. Configured in `/etc/dnf/automatic.conf` for **security updates only** (`upgrade_type = security`, `apply_updates = yes`, `download_updates = yes`) rather than the package default of all-updates. `systemctl enable --now dnf-automatic.timer` (runs once daily).
- **auditd**: already installed, active and enabled by default on this AMI. Not modified.
- **journald**: persistent logging already enabled (`/var/log/journal` exists and is populated). Not modified.
- No unexpected local logins found in `last`/`wtmp` — only the instance's own boot record.

## RouterOS config apply

Script: `routeros/apply-config.py`. Requires `pip install paramiko boto3`.

```powershell
# Apply to a live router (fetches password + WireGuard key from SSM automatically)
python routeros/apply-config.py 10.0.1.1 routeros/nyc/initial-config.rsc --ssm /home-platform/router/nyc-admin-password --wg-key-ssm /home-platform/wireguard/nyc-private-key

# First-time apply to factory-default router (provide factory password)
python routeros/apply-config.py 192.168.88.1 routeros/nyc/initial-config.rsc --ssm /home-platform/router/nyc-admin-password --ssh-password <factory-password> --wg-key-ssm /home-platform/wireguard/nyc-private-key
```

The script replaces `PLACEHOLDER` in the .rsc file with the real admin password from SSM, and (if `--wg-key-ssm` is given) `WG_PRIVATE_KEY_PLACEHOLDER` with the real WireGuard key. **It refuses to run at all if `WG_PRIVATE_KEY_PLACEHOLDER` is still present and `--wg-key-ssm` wasn't given** — see the Gotchas entry below for why.

**For a small targeted change to an already-live router (e.g. adding one DNS record), don't reapply the whole `.rsc` file.** SSH in directly (paramiko or PuTTY) and run just that one RouterOS command. Reapplying the full file re-runs its WireGuard section too, which is only safe with a real key supplied.

## Network topology

- NYC RB5009 (`/system identity` = `rt-nyc`, renamed 2026-07-11): LAN 10.0.1.1/24, WireGuard 10.0.3.2
- Rambles RB5009 (`/system identity` = `rt-rambles`, renamed 2026-07-11): LAN 10.0.2.1/24, WireGuard 10.0.3.3
- EC2 WireGuard hub: 10.0.3.1 (interface ens5, not eth0)
- Laptop: WireGuard 10.0.3.4 (re-provisioned 2026-07-07 — see "EC2 access" above)
- Nighthawk RS700: AP mode at 10.0.1.2 (connect via LAN port, not WAN)
- ZenWiFi AX6600 (Rambles): converted to AP mode 2026-07-04, hangs off the RB5009 same as the RS700 pattern
- Both routers' `forward` chains trust each other's LAN subnet (not just the WireGuard subnet) — real cross-site LAN traffic works, not just router-to-router. See "Gotchas" below for why this wasn't originally obvious.
- Each router's own admin services (`www`/`www-ssl`/`winbox`) trust the *other* site's LAN too, as of 2026-07-11 — e.g. rt-nyc's web UI/Winbox is reachable from `10.0.1.0/24` (its own LAN), `10.0.2.0/24` (Rambles LAN), and `10.0.3.0/24` (WireGuard), and rt-rambles mirrors that. `ssh` was deliberately left scoped to own-LAN + WireGuard only (not widened) — this was specifically about the web/Winbox admin UI, per Bill's request. **Two separate ACLs had to be widened, not just one** — the `/ip service set ... address=` restriction on the service itself is necessary but not sufficient; each router's own `/ip firewall filter chain=input` also has its own LAN/WireGuard-only accept rules ending in a catch-all drop, and that chain governs traffic destined *to the router itself* (distinct from the `forward` chain, which only governs traffic passing *through* it to other LAN hosts). Widening only the service ACL and not the input-chain rule silently does nothing — the packets get dropped by the firewall before reaching the service layer. Added a `cross-site-lan` accept rule (source = the other site's LAN) to both routers' input chains, placed before `drop-input`.

## Gotchas (learned the hard way)

- **AWS CLI from Git Bash mangles SSM parameter paths.** MSYS auto-converts leading-`/` arguments into Windows paths, so `aws ssm get-parameter --name /home-platform/...` silently fails with `ParameterNotFound` or a path-validation error from Git Bash. Use PowerShell for any AWS CLI call involving a path-style name (SSM, IAM paths, etc). **Not just AWS CLI — any argument starting with `/` passed to any program from Git Bash is at risk**, confirmed 2026-08-16 with a plain `python script.py "/ip dhcp-server lease print"` (RouterOS command syntax) run through paramiko: some strings passed through unmangled, others silently became `C:/Program Files/Git/ip dns static print` before Python ever saw `sys.argv` — inconsistent per-argument, not a blanket failure, which makes it easy to think the first success proves the pattern is safe. Verify with `python -c "import sys; print(repr(sys.argv))" "/your/arg"` if a leading-`/` argument's behavior is unverified from Git Bash; PowerShell doesn't have this problem at all. **This is specifically an MSYS/Git-Bash-on-Windows quirk — native Linux bash (NUCs, or a Linux workstation) has no such path mangling; leading-`/` arguments work normally there, no PowerShell-equivalent workaround needed.**

- **Granting a new IAM permission and using it in the same Terraform apply can fail.** If a commit both adds a permission (e.g. `cloudfront:*`) to the GitHub Actions role's policy *and* creates a resource requiring it, the apply may run before the policy change has propagated (IAM is eventually consistent, typically a few seconds). Symptom: `AccessDeniedException` right after a successful policy update in the same run. Fix is just to re-run — or apply locally once with admin creds, which is unaffected since it's a different principal.

- **Compose config changes need an explicit container restart.** Editing `prometheus.yml`, `loki-config.yaml`, etc. and re-deploying (`scripts/deploy-aws-stack.ps1`) updates the file on disk, but the running container doesn't reload it automatically — `docker compose restart <service>` is required. Grafana's dashboard *provisioning* is the one exception (polls its directory every 30s per `dashboards.yml`), but Grafana datasource/plugin config still needs a restart same as everything else.

- **A freshly added Route53 record can take up to 24h to resolve on some networks.** Route53's SOA negative-cache TTL is 86400s by default. If a hosted zone existed *before* a record was added (ours did — the zone predates `billandjessie.com`'s apex A record by weeks), any resolver that queried it in the gap may have cached "no record" and will keep serving that until the negative-cache TTL expires, even though the record is now live and correct everywhere else. Verify with `--resolve` (bypasses DNS) or a fresh resolver (e.g. cellular data instead of home WiFi) before assuming something's actually broken.

- **Cisco SG300 switches (sw-main, sw-desk) need `pip install "paramiko<3"` for SSH.** Paramiko 3.x fully removed SHA-1-based key exchange (not just deprioritized — the implementation is gone), and these ~2013-era switches only offer `diffie-hellman-group1-sha1`/`group14-sha1`. Symptom: `IncompatiblePeer: no acceptable kex algorithm`. Paramiko 2.12.0 still has it.

- **SG300 SSH auth happens *inside* the shell, not at the SSH protocol layer.** `transport.auth_none(username)` succeeds and `is_authenticated()` reports `True` with no password at all — the device instead prints a `User Name:` / `Password:` prompt once you open a shell channel, exactly like a Telnet session. Send credentials as if typing them, not via paramiko's `auth_password`/`auth_interactive` (both get rejected with `BadAuthenticationType`).

- **Containers on the EC2 hub can't reach IMDS by default.** The instance's metadata hop limit defaults to 1, which only covers the host itself — a container one hop further away via the Docker bridge (e.g. `cost-exporter`'s boto3 client) gets no response and silently falls back to no credentials. Fixed via `metadata_options.http_put_response_hop_limit = 2` in `terraform/aws/ec2.tf`. Any future container that needs the instance profile's credentials relies on this already being set.

- **SG300 web UI "Add" dialogs are unreliable via clicks (real or synthetic).** Its buttons are `<table class="btn_normal">` elements with an `onclick` attribute, not real `<button>`/`<input>` elements, inside a nested frameset. Neither `computer` tool clicks nor DOM `.click()` reliably opened the dialogs. If the web UI must be used, call the handler function directly — find it via `element.getAttribute('onclick')` (grep for the function name, e.g. `addRecord`) then invoke `frame.functionName()` in the correct child frame. In practice it was easier to enable SSH under Security → TCP/UDP Services (a normal checkbox, works fine via clicks) and do everything else via CLI.

- **SG300 `snmp-server community <string> ro <ip> view <name>` — the IP comes BEFORE `view`, not after.** `ro view Default 10.0.3.1` fails with `"Wrong number of parameters or invalid range, size or characters entered"`, even though `snmp-server community public ro ?` lists `view` and an IPv4 address as if they were addable together — they're actually alternative choices for the *same* next token, and only IP-then-view is valid. Learned this the hard way by running `no snmp-server community public` first (to clear the old unrestricted entry) and then hitting this error, which left SNMP completely unconfigured — not just unrestricted, fully broken — until corrected and confirmed against a live Prometheus scrape. If clearing an existing community first, verify the replacement command actually succeeds before moving on.

- **RouterOS `/system logging add topics=a,b,c` is AND, not OR.** A single rule listing multiple topics only fires for a log entry carrying *all* of them simultaneously — which never happens, since a message only ever has one severity topic at a time. Nothing gets forwarded and there's no error; it just silently never matches. Fix: one rule per topic, all pointing at the same action, exactly like RouterOS's own built-in default rules do (`topics=info action=X`, `topics=warning action=X`, etc. as separate lines).

- **Promtail's built-in syslog scrape target only supports RFC5424, not legacy BSD syslog (RFC3164).** RouterOS and Cisco SG300 both send RFC3164 by default. Symptom: `promtail_syslog_target_parsing_errors_total` increments with `"expecting a version value in the range 1-999"` — the packets *are* arriving (check this counter, not just `promtail_sent_entries_total`, before concluding otherwise), they're just failing to parse. Fix: don't use Promtail's syslog target at all — run `rsyslog` (handles both formats) as the actual UDP 514 receiver, have it write one file per source IP, and let Promtail just tail those files. See "Syslog receiver on EC2 hub" above.

- **AL2023's `fail2ban` package silently pulls in `firewalld` and `nftables` as RPM dependencies**, even though fail2ban's own default `banaction` is `iptables-multiport` (plain iptables, not firewalld). `dnf install fail2ban` leaves firewalld installed and **enabled** (though inactive) — if left that way, a future reboot would start firewalld, which manages its own nftables-based ruleset independently of Docker's iptables chains and risks breaking container networking. Fix: `systemctl disable firewalld` right after installing fail2ban (don't just check `is-active`, check `is-enabled` too). This was caught during the 2026-07-11 EC2 host security review.

- **~~A RouterOS 7.19.6 RB5009's own `/system logging` remote-syslog action can silently fail to transmit at all~~ — misdiagnosis, resolved 2026-07-11.** Originally (2026-07-04) confirmed via `tcpdump` on the receiving end *and* RouterOS's own `/tool sniffer` on the sending router, both showing zero packets sourced from the router's LAN address, even though the firewall's `output` chain counts them as sent. The real cause: the router's self-generated syslog is sourced from its **WireGuard interface address** (10.0.3.2/10.0.3.3), not its LAN address (10.0.1.1/10.0.2.1) — makes sense in hindsight, since that's the interface the route to the hub actually goes out. Nothing was ever broken; the wrong source IP was being monitored. See `docs/network-inventory.md`'s "Log collection" section.

- **`ansible.builtin.ini_file` doesn't exist** — INI-file editing lives in `community.general.ini_file`, which isn't installed on the EC2 control node (`deploy-nucs.ps1` only ensures `ansible-core`, no extra collections). Symptom: `couldn't resolve module/action 'ansible.builtin.ini_file'`. For simple single-key edits to a well-known default config (e.g. `/etc/dnf/automatic.conf`), `ansible.builtin.lineinfile` with a `regexp`/`line` pair per key avoids the extra collection dependency entirely.

- **Promtail's `docker_sd_configs` + `__path__` relabel trick (to force file-based tailing instead of Docker-API log streaming) never actually switches tailing modes — confirmed root cause 2026-07-11.** `docker_sd_configs` targets are always consumed by Promtail 3.3.2's Docker-API-streaming target type (`target.go`, logged as `"added Docker target"` / `"finished transferring logs"`), which never reads `__path__` at all; that field is only honored by targets built from `static_configs`/`file_sd_configs` (a real `FileTarget`, logged as `"tail routine: started"`). The SD targets showing as discovered/ready on `/targets` is not evidence it's working — check which log lines actually appear (`target.go` vs `tailer.go`), not just target state. This is a known Promtail limitation, not a config mistake. **Superseded 2026-07-11**: the glob-based `docker` job this led to (tailing `/var/lib/docker/containers/*/*-json.log` directly) worked but could only label by opaque `container_id`, not a friendly name — replaced by switching Docker's log driver to `journald` (see below) and retired entirely, since containers stop writing to their old json-file logs once recreated under the new driver.

- **Docker's default logging driver on the EC2 hub is `journald`, not the default `json-file`** — set via `/etc/docker/daemon.json` (host-level, not tracked in git, same caveat as nginx/certbot/rsyslog above: `{"log-driver": "journald"}`). Done 2026-07-11 so container logs get tagged with real container names (`CONTAINER_NAME`/`CONTAINER_ID_FULL` journal fields) for free through Promtail's existing `journal` job, instead of needing Docker socket/API access (which was deliberately avoided after it caused a full-history-replay bug — see the `docker_sd_configs` entry above). **The log driver is fixed at container-*create*-time, not read live from the daemon** — editing `daemon.json` and restarting the `docker` service does NOT retroactively change already-running containers (confirmed via `docker inspect <container> --format '{{.HostConfig.LogConfig.Type}}'` still showing `json-file` after a daemon restart alone). Every container must be recreated to pick up a daemon-level log-driver change: `docker compose up -d --force-recreate` (not just `up -d`, which only recreates containers whose own service definition changed). Promtail's `journal` job relabels `__journal_container_name` → `container` and `__journal_container_id_full` → `container_id` (see `compose/aws/promtail/promtail-config.yaml`) — these fields are only present on container-sourced journal entries, absent on genuine host service entries, which is fine (Loki treats a missing label as absent, not an error).

- **`docker compose build` on the EC2 hub failed with `compose build requires buildx 0.17.0 or later`** even though Docker itself (25.0.16, AL2023's own `docker.x86_64` package) was current — AL2023's docker package doesn't pull in a buildx plugin as a dependency the way `docker-ce` does, so whatever buildx binary is present is whatever was manually dropped in at some point (found: v0.12.1, manually placed at `/usr/libexec/docker/cli-plugins/docker-buildx`, no RPM owns it). This only bites `deploy-aws-stack.ps1`'s `docker compose build` step (for `cost-exporter`, the one image built from a local Dockerfile) — `docker compose pull`/`up` for the other prebuilt images works regardless of buildx version. Fixed 2026-07-11 by downloading `docker/buildx` v0.35.0 from its GitHub releases (checksum-verified against the release's published `checksums.txt`) and replacing the binary in place (old one backed up alongside as `docker-buildx.bak-0.12.1`). Not tracked by any package manager — if the instance is ever rebuilt, redo this manually (same caveat as nginx/certbot/rsyslog above), or fix by installing a real `docker-buildx-plugin` RPM if AL2023 ever ships one.

- **EC2 hub's WireGuard interface (`wg0`) had no explicit `MTU` set in `/etc/wireguard/wg0.conf`, so `wg-quick` auto-derived it from the underlying `ens5` interface (9001, AWS's jumbo-frame default) minus overhead — 8921.** Both site routers' own `wg-aws` interfaces are correctly set to MTU 1420. This asymmetry went unnoticed because every EC2↔NYC/Rambles integration before nuc4 was UDP (SNMP scraping) or ICMP (ping) — small, single-packet exchanges that never touch MTU limits. The first TCP-heavy flow (Ansible SSH from EC2 to nuc4 during initial provisioning, 2026-07-13) exposed it: TCP handshake and small packets worked fine, but the ~1390-byte segment carrying EC2's own SSH KEXINIT never arrived at nuc4 (confirmed via `tcpdump` on nuc4 — a real gap in the byte stream, SACK'd but never retransmitted). EC2 believed the tunnel could carry frames up to 8921 bytes, built a segment sized against nuc4's advertised 1460 MSS, but once WireGuard's own encapsulation overhead was added the real on-wire packet exceeded NYC's actual internet path MTU (~1500) and was silently black-holed — no ICMP "fragmentation needed" made it back, so EC2's path-MTU-discovery never corrected itself, and retries kept resending the same doomed size. Fixed by adding `MTU = 1420` to `/etc/wireguard/wg0.conf` on EC2 and `systemctl restart wg-quick@wg0` (brief blip for all three peers: nyc, rambles, laptop). Host-level config, not tracked in git — same caveat as nginx/certbot/rsyslog above, redo manually if the instance is ever rebuilt. If a *new* TCP-based integration to either site ever mysteriously hangs past the initial handshake again, check this first before assuming it's an application-layer bug.

- **Reapplying a site's `initial-config.rsc` to an already-live router can take down its WireGuard tunnel — happened for real on 2026-07-17.** The WireGuard section (`/interface wireguard peers remove`, `/ip address remove`, `/interface wireguard remove`, then re-`add`) is written for a factory-default router with no tunnel yet — it unconditionally tears down `wg-aws` before recreating it. `apply-config.py` only ever substituted `PLACEHOLDER` (the admin password); the WireGuard section uses a separate token, `WG_PRIVATE_KEY_PLACEHOLDER`, that was meant to be hand-substituted first. Running `apply-config.py` straight against a live router for an unrelated one-line change (adding a DNS record) uploaded the file with the literal placeholder string still in it, which tore down Rambles' live `wg-aws` interface/peer/address and then failed to recreate it (`invalid private key`) — a real outage of the site-to-site tunnel, not just a no-op. Recovered by fetching the real key from SSM (`/home-platform/wireguard/rambles-private-key`) and manually re-running the interface/address/peer/route `add` commands over a direct SSH session (not another full-file reapply). **Fixed 2026-07-17**: `apply-config.py` now takes `--wg-key-ssm` to substitute the real key, and refuses to run at all (exits before connecting) if `WG_PRIVATE_KEY_PLACEHOLDER` is still in the content and `--wg-key-ssm` wasn't given. The underlying lesson still stands regardless of the script guard: **a full `.rsc` reapply is for first-time/factory setup; a small change to a live router should be a single targeted command over a direct SSH session, not a full-file reimport.**

- **RouterOS's `/ip dhcp-server lease add` for a MAC the DHCP server has never actually seen creates a reservation that only takes effect once that MAC's client is offered/renews a lease** — until then it just sits as `status=waiting, last-seen=never`, which looks alarming but is normal (same pattern hit with `furry` and `hue-nyc`, and again with nuc4's original MAC-mismatch fix). A device already holding a *different* dynamic lease (e.g. from the pool) needs an explicit `dhclient -r && dhclient` (or a reboot) on the client side to pick up the new reservation — RouterOS won't proactively renegotiate an active lease.

- **Adding a new AWS service to the `github_actions` role's Terraform-managed IAM policy needs every permission enumerated up front — a new service almost never needs just its own `service:*` actions.** Adding the Postgres-backups DLM policy (2026-07-18) took four separate PRs to fully unblock: (1) `iam:CreateRole` for the new execution role wasn't scoped to it (easy to miss — the role is new, so it can't be in an existing resource list); (2) fixing that with a direct resource reference created the same circular-dependency bug as the ansible-deploy bucket (see the S3 circular-dependency entry above) — needed a literal ARN string instead of `aws_iam_role.dlm.arn`; (3) breaking that cycle removes all Terraform-graph ordering between the policy update and the role's creation, so the *next* apply hit a genuine IAM eventual-consistency race (same class as the `cloudfront:*` entry above) — needed an explicit `time_sleep` + `depends_on`; (4) even with the role created, `github_actions` had zero `dlm:*` API permissions at all (an outright oversight, not a variant of 1-3) — `dlm:CreateLifecyclePolicy`/`TagResource`/etc. had to be added separately; (5) `CreateLifecyclePolicy` also needs `iam:PassRole` on the new execution role's ARN specifically — DLM (like most AWS services that assume a role on your behalf) requires the *caller* to be allowed to hand that role off, which is easy to forget since it's an `iam:*` action gating a different service's API call, not a `dlm:*` action. **When wiring up a new AWS service for the first time: grant role creation (with a literal ARN + time_sleep if it's IAM referencing itself), the service's own CRUD actions, tagging actions if the resource supports tags, and `iam:PassRole` on any execution role it assumes — check the AWS docs' IAM policy reference for the service up front instead of discovering each one via a failed apply.**

- **A `.gitattributes` rule scoped to `*.sh` doesn't catch extension-less files that also need LF line endings** — `compose/aws/postgres-backup/crontab` (a cron.d file, no extension) got silently CRLF'd by a Windows `git checkout` despite being written as LF, since no attributes rule matched it. A cron.d line with a trailing CR embeds it in the command string (e.g. `...2>&1\r`), which can break the command or its redirection depending on the cron implementation — confirmed live via `crontab -l | cat -A` showing `^M` before every newline. Fixed by adding an explicit path-scoped rule (`compose/aws/postgres-backup/crontab text eol=lf`) rather than trying to generalize the `*.sh` pattern. **Separately: fixing the `.gitattributes` rule alone isn't enough if the file is already checked out** — `git add --renormalize` fixes the git-index blob (what gets committed), but the actual working-tree file on disk stays CRLF until the next explicit checkout, since `core.autocrlf=true` only converts LF→CRLF on checkout, it doesn't retroactively fix files already sitting in the working tree. Any script that deploys from the working tree directly (like `deploy-aws-stack.ps1`, which `scp`s local files, not a git checkout) will keep shipping the corrupted version until the on-disk file itself is rewritten — verify with `file <path>` (looks for "with CRLF line terminators") on the actual working-tree file, not just the staged blob.

- **The gh CLI's stored fine-grained PAT (`/home-platform/github/api-token`) cannot create new repos, cannot change a repo's OIDC subject-claim customization, and cannot trigger `workflow_dispatch` via the API** — all three hit for real while onboarding the `todo-app` repo (2026-07-26), all three failing with the same `403 Resource not accessible by personal access token` shape. Fine-grained PATs need repo-level "Administration: write" for the first two (repo creation is actually account/org-level, not grantable per-repo at all in most configurations) and "Actions: write" for the third — this token evidently has neither, though it does have enough for `git push`, `gh run list`/`gh run view`, and reading repo state. Workarounds used: (1) ask Bill to create the empty repo via the web UI, then push to it; (2) don't bother fixing the OIDC subject-claim format at the repo level — just trust both possible formats in the IAM trust policy (see the next entry); (3) push a real commit to retrigger a workflow instead of `gh workflow run`. If this token's scope is ever regenerated, consider granting these explicitly rather than rediscovering the gap per new repo.

- **A newly-created GitHub repo's OIDC subject claim can default to a different format than an older repo's** — confirmed via CloudTrail 2026-07-26: `todo-app` (created that day) presented `repo:bcalaway@37939549/todo-app@1313063209:ref:refs/heads/main` to AWS STS, not the plain `repo:bcalaway/todo-app:ref:refs/heads/main` format `nyc_pa_aws_gitops`'s own `github_actions` role (created 2026-07 earlier) has always used and that every IAM trust policy in this repo assumed. GitHub apparently changed the default to an immutable owner/repo-ID format for new repos at some point; the repo-level API to force the classic format back (`PUT /repos/{owner}/{repo}/actions/oidc/customization/sub`) needs Administration write the stored PAT doesn't have (see the entry above). Symptom: `AssumeRoleWithWebIdentity` fails with a plain `AccessDenied`/`Not authorized`, and the `aws-actions/configure-aws-credentials` action's own retry-with-backoff makes it look like a transient/networking problem for a couple of minutes before the real error surfaces — check CloudTrail's `AssumeRoleWithWebIdentity` events (`Username` field shows the exact `sub` presented) rather than assuming it's flaky. Fixed by trusting both formats in the `StringLike` condition (`terraform/aws/apps.tf`) rather than fighting the repo setting. **Check this for every future app's first CI run, don't assume the plain format.**

- **Postgres 15+ changed the default `public` schema privileges — granting `GRANT ALL PRIVILEGES ON DATABASE` to a new role is not enough for that role to `CREATE TABLE` in its own database.** Only the database owner (or a role explicitly granted `CREATE` on the `public` schema) can create objects there by default now; earlier Postgres versions granted `CREATE`/`USAGE` on `public` to the `PUBLIC` pseudo-role automatically, which is what made the old "just grant database privileges" pattern work. Hit while onboarding `todo-app`'s database (2026-07-26): `CREATE DATABASE`/`CREATE ROLE`/`GRANT ALL PRIVILEGES ON DATABASE` all succeeded, but the app's own `Base.metadata.create_all()` would have failed at first boot with a permissions error had it not been caught first via a manual `CREATE TABLE probe` test as part of onboarding. Fixed with `ALTER DATABASE "<app>" OWNER TO "<app>";` plus `ALTER SCHEMA public OWNER TO "<app>";` (run after connecting to that database specifically, not the default `postgres` database) — makes the app's own role the owner of both, matching how a single-tenant app's database is conventionally set up. **Add both `ALTER` statements to the standard per-app database onboarding steps in `docs/app-platform.md`, not just `CREATE DATABASE`/`CREATE ROLE`/`GRANT` — and verify with a real `CREATE TABLE` test as the app's own role before considering onboarding done, don't just trust the grants.**

- **A `uvicorn`/Node/any-framework app behind Traefik will build its own external URLs (OIDC `redirect_uri`, etc.) using the *internal* plain-HTTP scheme it actually sees, not the real external HTTPS scheme, unless it's explicitly told to trust the proxy.** Traefik terminates TLS and proxies to the app over plain HTTP on the internal Docker network; a framework's "what scheme am I being served over" detection (Starlette's `request.url_for()`, etc.) reflects that internal hop, not `X-Forwarded-Proto`, unless proxy-header trust is turned on. Hit for real on `todo-app` (2026-08-20): `/login`'s redirect_uri came out as `http://` instead of `https://`, which Authentik's strict `redirect_uris` match rejected outright with "Redirect URI Error" — a fully broken login that had shipped silently until someone actually clicked through it. Fixed for uvicorn/FastAPI apps with `--proxy-headers --forwarded-allow-ips='*'` on the uvicorn `CMD` (safe here since Traefik and the app share the same internal Docker network, not a public untrusted hop). **When building a new language template or any app that constructs its own absolute external URLs (OIDC callbacks, webhooks, canonical links): either explicitly configure the framework to trust forwarded-proto from Traefik, or build the URL from `APP_NAME`/a hardcoded `https://` scheme instead of trusting request introspection — don't assume the first login attempt against a fresh app will just work, actually click through it.** (The Node and C++ templates sidestep this entirely by constructing `redirect_uri` from `https://${APP_NAME}.billandjessie.com/...` directly rather than deriving it from the incoming request.)

- **Node's common `import.meta.url === \`file://${process.argv[1]}\`` "run as main module" idiom silently never matches on Windows**, because `process.argv[1]` is a raw Windows path (`C:\app\server\index.js`, backslashes, no leading slash) while `import.meta.url` is a real `file:///C:/app/server/index.js` URL — the string comparison just fails, `listen()` never runs, and the process exits clean with **no error and no output at all**, which looks like nothing happened rather than like a crash. Works fine in the Linux container in production (Linux paths already start with `/`, so the naive concatenation happens to form a valid URL there), so this only bites local Windows dev — confirmed for real building the React template (2026-08-20) on this workstation. Fix: `import.meta.url === pathToFileURL(process.argv[1]).href` (from `node:url`), which normalizes both platforms correctly. Check this first if a Node script that "should" print a startup log or bind a port just silently exits 0 when run locally on Windows.

- **vcpkg's `libpq` port (pulled in transitively by `libpqxx`) needs `bison` and `flex` to build from source, and a minimal `build-essential`/`clang`/`cmake` toolchain image won't have them.** Confirmed for real building the C++ template's Docker image (2026-08-20): `vcpkg install` got most of the way through the dependency tree (grpc, protobuf, openssl, etc. all built fine) before failing specifically on `libpq` with `Could not find bison`. Fixed by adding `bison flex` to the base image's `apt-get install` line — see `templates/cpp/Dockerfile`. If any future C++ (or other native-toolchain) Dockerfile adds a Postgres client dependency, budget for this up front rather than discovering it after everything else already built.

- **CMake 3.28+'s Ninja generator scans for C++20 modules by default whenever `CMAKE_CXX_STANDARD` is 20 or higher — even in a project that doesn't use modules at all — and the scanning machinery breaks clang-tidy.** It injects a `@some_file.cpp.o.modmap` response-file argument into that translation unit's entry in `compile_commands.json`; clang-tidy re-executes the recorded command directly and doesn't understand that argument, failing with `no such file or directory: '...modmap'` on every file it touches. Confirmed for real building the C++ template (2026-08-20): 6 of 7 source files failed lint over this before the fix. Fix: `set(CMAKE_CXX_SCAN_FOR_MODULES OFF)` in `CMakeLists.txt` for any C++20+ project that isn't actually using modules — see `templates/cpp/CMakeLists.txt`. Worth checking for on any future CMake+clang-tidy work in this repo, not just this template.

- **Docker Desktop on Windows requires an admin-elevated `wsl --install` + reboot that a sandboxed/non-elevated shell cannot self-serve** — `wsl --install` from a non-admin PowerShell just prints the "WSL is not installed, run wsl --install" hint over and over regardless of arguments, with no indication that the real blocker is missing elevation, not a bad command. Confirmed 2026-08-20 setting up this workstation for the React/C++ template work: Bill had to run the elevated install + reboot himself. Separately, **Docker's own containers run inside a second, hidden WSL2 distro named `docker-desktop`** (confirmed via `wsl --list --verbose`), not the user-visible `Ubuntu` distro installed alongside it — `wsl` (no `-d` flag) drops into the wrong one. To watch build/compile activity live from a plain terminal: `wsl -d docker-desktop` then `top`. Docker Desktop's own GUI also has a "Builds" tab with live log streaming, no WSL knowledge required. And the Docker daemon itself doesn't come up just because Docker Desktop is installed — the GUI app has to actually be launched at least once (`Start-Process "C:\Program Files\Docker\Docker\Docker Desktop.exe"`) before `docker` commands stop failing with "cannot connect to the Docker daemon."

- **Rocky Linux 10 + EPEL10 has no `ninja-build` package at all yet, and building the C++ template's vcpkg dependency tree locally (not in Docker) hits a different set of gaps than the Dockerfile does.** Confirmed for real 2026-08-20 testing all three app templates end-to-end on a Rocky 10.2 box. The only ninja-compatible EPEL10 package is `samurai`, which installs its binary as `/usr/bin/samu`, not `ninja` — CMake's `-G Ninja` generator looks for a binary literally named `ninja` and fails outright without a `~/.local/bin/ninja` symlink to it (same `~/.local/bin`-on-PATH pattern already used for the NvChad Neovim install). Separately, vcpkg's own `bootstrap-vcpkg.sh` needs `zip`/`unzip` (not present by default, unlike `curl`/`tar`), and the `openssl` port's `Configure` script needs several perl core modules (`IPC::Cmd`, `FindBin`) that Rocky 10's default minimal `perl-interpreter` package doesn't include — installing the full `perl` metapackage up front avoids finding these one at a time. `bison`/`flex` are needed here too, same root cause as the existing Dockerfile gotcha below, just hit locally instead of in the container image. Full package list and the vcpkg clone/bootstrap steps are now in `docs/new-machine-setup.md`'s "Install app-template development tooling" section — follow that rather than rediscovering this per machine. The actual vcpkg dependency build (grpc/protobuf/openssl/libpqxx from source) took ~26 minutes on a cold cache once all of the above was fixed, matching the template README's own "genuinely slow" warning.

- **`deploy-nucs.ps1` silently ran the *stale* copy of `ansible/` on the hub for at least one prior deploy, because `scp` failed and the script didn't notice.** Root cause, confirmed live 2026-08-20 deploying `hue`'s agent (Milestone 12): three files under the hub's `~/home-platform/ansible/` (`site.yml`, one NUC's block in `inventory/hosts.yml`, `routeros.yml`) were owned by `root`, not `ec2-user`, from some earlier operation — `scp` (running as `ec2-user`) failed with `Permission denied` on every file in `ansible/` as a result. The script kept going anyway and ran `ansible-playbook site.yml` against whatever was already there, which "succeeded" in the sense that Ansible itself reported no errors — it just never saw the new role that had supposedly just been added. **Root cause of the silent continuation**: `$ErrorActionPreference = "Stop"` (already set) only governs PowerShell cmdlets, not native processes like `ssh.exe`/`scp.exe` — a non-zero exit from those doesn't stop a PowerShell script on its own, script has to check `$LASTEXITCODE` explicitly. Fixed 2026-08-20: `deploy-nucs.ps1` now checks `$LASTEXITCODE` after every `ssh`/`scp` call and throws immediately if one fails. The `.sh` counterpart was never affected — `set -euo pipefail` genuinely does abort on any non-zero exit from external commands, unlike PowerShell's error preference. **If a future Ansible run "succeeds" but a change you just made clearly isn't taking effect, check file ownership on the hub's `~/home-platform/ansible/` before assuming the logic itself is wrong.**

- **A Docker network can exist in `docker network ls` and still make every container attach to it fail with "network ... does not exist."** Hit for real on `nuc4` deploying `hue`'s agent (2026-08-20, unrelated to that deploy — this was the *pre-existing* exporter stack): `docker compose up -d` failed with `failed to create endpoint speedtest-exporter on network nuc_default: network <id> does not exist`, even though `docker network ls` showed that exact network ID present, and the failure was consistent across containers/retries, not transient. The three exporter containers were left sitting in `Created` (never `Running`) state, which is what let this go unnoticed since the containers technically "existed" but weren't serving anything. Fix: `docker compose down` (removes the containers and the corrupted network cleanly) then `docker compose up -d` (recreates the network fresh) — not `docker network rm` first, since a plain `down` already handles both in the right order. If a NUC's exporter/app containers are stuck in `Created` state rather than `Running`, check for this before assuming it's a config problem.

- **A site's own NUC makes a working jump host for reaching that site's router admin SSH when you're off-site or on the other site's LAN.** `rt-nyc`'s and `rt-rambles`' SSH services are deliberately scoped to own-LAN + WireGuard only, not cross-site LAN (see "Network topology" above) — the weather-station DNS rename (2026-08-21) needed a live change on `rt-nyc` from a session physically on the Rambles LAN, with no WireGuard tunnel active. Direct SSH to `10.0.1.1` failed the way it always does from off-network (TCP connects, but the router drops the connection before sending an SSH banner — RouterOS's service ACL rejects at the application layer, not the TCP layer, so `Test-NetConnection`/a bare port check looks like success and is misleading here). Fix: SSH to `nuc4` (10.0.1.34, on `rt-nyc`'s own LAN, reachable directly since the routers' forward chains already trust each other's LAN subnet) as a bastion, then open a `direct-tcpip` channel from that session to `10.0.1.1:22` and complete the RouterOS SSH handshake through it (paramiko's jump-host pattern: `transport.open_channel("direct-tcpip", (dest_host, 22), (bastion_host, 0))`, passed as `sock=` to a second `SSHClient.connect()`). No sshpass or interactive shell needed on the NUC itself. Same pattern works in reverse (via `nuc5` to reach `rt-rambles`) whenever the session is on the NYC LAN instead.

- **Rachio's classic public API (`api.rach.io/1/public`, what the `rachiopy` library wraps) returns `devices: []` for a Rachio Smart Hose Timer account, even with a valid, working API key.** Confirmed live 2026-08-23 onboarding `rachio-exporter`: `/person/info` and `/person/{id}` both authenticated fine (right account, right email) but `person.devices` was unconditionally empty — not an account-linkage problem. The Smart Hose Timer (a WiFi "base station" puck + battery-powered valves, not the classic wall-mounted wired controller) lives entirely on a **separate, mostly third-party-wrapper-unsupported API**: host `cloud-rest.rach.io` (not `api.rach.io`), `GET /valve/listBaseStations/{personId}`, `GET /valve/listValves/{baseStationId}`, `POST /summary/getValveDayViews` for history — same `Authorization: Bearer <key>` auth, completely different object model (`BaseStation`/`Valve`/day-view summaries vs. the classic API's `device`/`zone`). If a Rachio integration ever returns empty devices despite a valid key, check which product line is actually installed before assuming the account/token is misconfigured. Separately: `getValveDayViews`'s per-run `skip` field is easy to miss — a rain/weather-intelligence-skipped run still reports its originally-*planned* `durationSeconds`, so duration alone overcounts skipped runs as real watering; only a run with no `skip` key and `durationSeconds > 0` is confirmed to have actually happened.

- **nuc5's onboard Bluetooth was completely dead — `bluetoothctl list` showed no controller at all — because RHEL/Rocky's `linux-firmware` package excludes some MediaTek blobs for licensing reasons that Ubuntu/Fedora's builds do include.** `dmesg` showed `Direct firmware load for mediatek/BT_RAM_CODE_MT7922_1_1_hdr.bin failed with error -2` (file simply absent from `/usr/lib/firmware`), found 2026-08-21 while investigating Bluetooth range for propane-tank (Mopeka) sensor monitoring. Fixed by fetching the blob directly from the upstream `linux-firmware` git repo (`https://git.kernel.org/.../mediatek/BT_RAM_CODE_MT7922_1_1_hdr.bin`) and dropping it at that exact path. **Module reload order matters**: `modprobe -r btmtk` before `btusb` fails with `Module btmtk is in use` — `btusb` (the USB HCI transport) depends on `btmtk` (MediaTek's firmware loader), so remove `btusb` first, then `btmtk`, then `modprobe btusb` to bring both back. **Persisted in Ansible** (`ansible/roles/base/tasks/main.yml` + its new `handlers/main.yml`) so any NUC with an MT7922 card (detected via `lspci`, not hardcoded to nuc5) gets this automatically — nuc4 hasn't actually been re-provisioned against this yet since its WireGuard tunnel has been down for the whole span of this fix; check `bluetoothctl list` on it once that's back and a deploy has run. Verified live on nuc5 via `bluetoothctl list` showing a real controller MAC afterward, then a real `bluetoothctl scan on` picking up nearby BLE advertisements (Apple/Bose devices, no Mopeka signature in range from that location).

- **Grafana's generic_oauth login failed against Authentik with no visible error to the user — just landing back on Grafana's own login page.** First surfaced 2026-08-22 via Grafana's own container logs (`docker logs grafana`): `level=error msg="Error getting email address" url=https://auth.billandjessie.com/application/o/userinfo//emails ... 404`. Grafana's OAuth handler falls back to a GitHub-specific `{api_url}/emails` sub-endpoint whenever it can't extract a non-empty email from the primary userinfo response — Authentik has no such endpoint at all, so the fallback 404s and that login attempt fails. **First fix attempt was wrong** (or at least incomplete): assumed Grafana just wasn't *finding* the email in the response and set `GF_AUTH_GENERIC_OAUTH_EMAIL_ATTRIBUTE_PATH: "email"` — this didn't help (confirmed live: same exact error recurred afterward, and the env var was confirmed present on the running container, ruling out a deploy issue). **Actual root cause**, found 2026-08-23 by checking Authentik's own `email` scope mapping (`ak shell`: `authentik_providers_oauth2.models.ScopeMapping`, expression `return {"email": request.user.email, ...}`) against the real user records (`authentik_core.models.User`): there are two separate Authentik accounts, `akadmin` (bootstrap admin, `email` correctly `bcalaway@gmail.com`) and `bcalaway` (the account actually used day-to-day, `email` was a blank string). An empty `email` claim is indistinguishable from "no email attribute at all" to Grafana's extraction logic, so it fell into the same broken `/emails` fallback regardless of `email_attribute_path`. Fixed by setting the `bcalaway` user's `email` field directly (`User.objects.get(username="bcalaway").email = "bcalaway@gmail.com"; .save()`) — not a config file, a one-off user-profile data fix, so it isn't GitOps-tracked and would need to be redone if that user record is ever recreated. The `email_attribute_path` config change is harmless and can stay, but wasn't the actual fix. **If any other app's generic-OAuth-style login against Authentik ever shows this same "silently bounces back to login" symptom, check the actual Authentik user's `email` field is non-empty before assuming it's a Grafana/app-side config or a redirect_uri/cookie/session problem — an empty claim looks like a missing one from the relying party's side.**

- **Two deploys of the same app pushed within seconds of each other can race on the hub and corrupt the deploy itself, not just slow things down.** `app-deploy.yml`'s hub-side script is always written to and executed from the same path, `/tmp/deploy.sh` — nothing scopes it per run. Hit for real 2026-08-22: two `hue` commits pushed ~30s apart both triggered `hub-deploy`, and the second run's deploy failed with `/tmp/deploy.sh: line 5: .amazonaws.com: command not found` — a truncated fragment of the ECR registry URL, meaning the two runs' `echo ... | base64 -d > /tmp/deploy.sh` writes overlapped and one script's content bled into the other's. (Milestone 14's "concurrent CD runs" gotcha earlier only described this as a *slowness* problem from resource contention — this is the same root cause producing an outright failure instead, on a single m5-class hub with no actual resource pressure that time.) Fixed by adding a `concurrency: {group: app-deploy-${{ inputs.app_name }}, cancel-in-progress: false}` block to `app-deploy.yml`'s `deploy` job, so same-app deploys queue instead of overlapping; different apps still deploy fully in parallel since the group key is per `app_name`. If a deploy ever fails with a shell error pointing at a mangled/truncated command inside `/tmp/deploy.sh`, check for a second concurrent run of the same app before assuming the script itself is wrong — `gh run rerun <run-id> --failed` after the other run finishes is enough to recover, no manual hub-side cleanup needed.

- **`ansible/roles/base/`'s MT7922 Bluetooth-firmware check (added 2026-08-21, see the entry above) called `lspci` but never added `pciutils` to the baseline package list** — so `ansible-playbook site.yml` failed outright, fatally, on both NUCs the next time it ran fresh (2026-08-24), before ever reaching any later role including `hue-agent`. It had presumably worked during the original fix because `pciutils` happened to already be present on nuc5 from some earlier manual install, never actually needed by the role itself. This silently blocked a queued `hue-agent` redeploy to nuc4 for days. Fixed by adding `pciutils` to `ansible/roles/base/tasks/main.yml`'s baseline package list. **Lesson: a task that "worked" during original development on one already-hand-tweaked host isn't proof the role is self-sufficient — check every command the role runs resolves to a package the role itself installs, not one that happens to pre-exist on the host it was written against.**

- **`ansible/roles/hue-agent/`'s image-transfer gate (fixed once already for nuc5, see Milestone 12's Gotchas entry above) was still broken in a subtler way, confirmed live 2026-08-24.** The nuc5 fix added a check for whether the target NUC has *an* image tagged `hue-agent:latest` at all (`docker image inspect ... ; rc != 0`) — but that only checks tag presence, not which actual image the tag points to. nuc4 had a `hue-agent:latest` tag from its original 2026-08-20 deploy that was never updated since; every later `deploy-nucs.ps1` run found the hub's own pull already cached (nothing "Downloaded newer image" to report, since some other NUC's deploy had already pulled the current tag to the hub) AND found nuc4's stale tag present, so both halves of the transfer gate passed and the actual (much older) image was never re-transferred. nuc4 silently ran a build 4+ days and one full milestone stale despite being "redeployed" multiple times in between. Fixed by comparing the actual image ID (`docker image inspect --format='{{.Id}}'`) between the hub's freshly-pulled image and whatever the NUC currently has, transferring whenever they differ (or the NUC has nothing at all) rather than checking mere tag presence. **If a NUC-deployed container's behavior doesn't match what CI just shipped, check `docker inspect <container> --format '{{.Created}} {{.Image}}'` directly on the NUC against the image ID in ECR before assuming the deploy script's own "already up to date" logic is trustworthy — it has now been wrong twice in two different ways.**

- **Grafana's OAuth login started failing again 2026-08-24, on most computers but not the one that had a long-lived session cookie already — `Login failed, User sync failed`, with `logger=user.sync msg="Failed to create user" error="user already exists"` in Grafana's own logs.** Root cause, found live via `ak shell`: Grafana links an OAuth login to a Grafana user by the IdP's stable external ID (`auth_id`, from the `sub` claim — Authentik's `User.uid`), not by email. The `akadmin`/`bcalaway` shared-email situation from the entry above bit a second time: at some point a login via `akadmin` got linked to Grafana's `bcalaway@gmail.com` user first (Grafana's `user_auth` table stored `akadmin`'s `.uid` as the `auth_id`, dated 2026-08-21). Every subsequent real login via the actual `bcalaway` Authentik account presents *that* account's own `.uid` — a genuinely different value, confirmed by comparing both accounts' `.uid` fields directly (`ak shell`: `User.objects.get(username=...).uid`) against the exact `auth_id`s in Grafana's error logs and its `user_auth` SQLite table (copied out of the `compose-aws_grafana-data` volume and queried with a throwaway `keinos/sqlite3` container, since the `grafana` container itself has no `sqlite3` binary). Grafana refuses to silently re-link an OAuth identity to an existing user by default (a real security safeguard, not a bug on its own) — hence the hard failure instead of just working. Fixed with `GF_AUTH_OAUTH_ALLOW_INSECURE_EMAIL_LOOKUP: "true"` (`compose/aws/docker-compose.yml`), which makes Grafana fall back to email-based matching when the stored external ID doesn't match — the intentionally-permissive setting is actually correct here since `akadmin`/`bcalaway` are genuinely the same person. Verified live: Bill confirmed login succeeded from a previously-failing machine after redeploy. **If Grafana's `user_auth` linkage ever seems to point at the wrong identity again, check both Authentik accounts' `.uid` values before assuming it's a fresh instance of the email-attribute bug from the entry above — same two accounts, different failure mode.**

[defaults]
inventory = inventory/hosts.yml
remote_user = bcalaway
private_key_file = ~/.ssh/ansible-nuc
host_key_checking = False
interpreter_python = auto_silent

[privilege_escalation]
become = True
become_method = sudo
become_ask_pass = False

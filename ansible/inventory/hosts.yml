all:
  children:
    nucs:
      hosts:
        nuc5:
          ansible_host: 10.0.2.10
          site: rambles
        # nuc4 (NYC): add once Rocky Linux 10 is installed — see docs/roadmap.md Milestone 8

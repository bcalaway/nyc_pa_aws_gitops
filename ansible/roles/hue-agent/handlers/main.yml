---
- name: Reload firewalld
  ansible.builtin.command: firewall-cmd --reload
  changed_when: true

- name: Restart hue-agent
  ansible.builtin.command:
    cmd: docker compose up -d --force-recreate
    chdir: /opt/compose/hue-agent
  changed_when: true

---
- name: Reload firewalld
  ansible.builtin.command: firewall-cmd --reload
  changed_when: true

- name: Restart mopeka-exporter
  ansible.builtin.command:
    cmd: docker compose up -d --build --force-recreate
    chdir: /opt/compose/mopeka-exporter
  changed_when: true

# Copy to secrets.yaml (gitignored) before running `esphome compile`/`upload`.
# Never commit the real secrets.yaml.
wifi_ssid: "REPLACE_ME"
wifi_password: "REPLACE_ME"
# Random 32-byte base64 key -- generate with:
#   python3 -c "import secrets, base64; print(base64.b64encode(secrets.token_bytes(32)).decode())"
api_encryption_key: "REPLACE_ME"
ota_password: "REPLACE_ME"
fallback_ap_password: "REPLACE_ME"
